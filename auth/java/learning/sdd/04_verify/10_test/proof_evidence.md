# Proof 증거 (retained)

게이트: `./gradlew test` (= contract.json `commands.proof`)

```
[proof] PASS · 10/10 passed -> tmp/proof-results.json
```

회귀 게이트 재실행: `./gradlew uiParity` → PASS

> 머신 산출: `tmp/proof-results.json` (빌드 시 생성, 방금 실행한 결과와 동일).
